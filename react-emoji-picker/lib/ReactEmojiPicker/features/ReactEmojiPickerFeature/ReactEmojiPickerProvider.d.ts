import { FC } from "react";
declare type ReactEmojiPickerProviderProps = {
    onSelected?: (emoji: string) => void;
};
declare const ReactEmojiPickerProvider: FC<ReactEmojiPickerProviderProps>;
export default ReactEmojiPickerProvider;
